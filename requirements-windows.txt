# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ☕ BEAN & BREW 카페 주문 시스템  — Windows 전용
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# [권장 환경]
#   Windows 10 / 11 (64-bit)
#   Python 3.10 ~ 3.13   (PyAudio 0.2.14+ 는 3.13 휠 지원)
#
# [설치 방법 — 1단계]
#   pip install -r requirements-windows.txt
#
# [설치 방법 — PyAudio 설치 실패 시]
#   ① pipwin 방식 (권장)
#       pip install pipwin
#       pipwin install pyaudio
#   ② 비공식 휠 직접 다운로드
#       https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
#       pip install PyAudio-0.2.xx-cpXXX-cpXXX-win_amd64.whl
#
# [TTS 관련 — 별도 설치 불필요]
#   pyttsx3 는 Windows SAPI5 를 자동 사용하며,
#   코드 내부에서 PowerShell System.Speech 로 자동 폴백됩니다.
#   한국어 음성 (Microsoft Heami 등) 은 Windows 설정 →
#   시간 및 언어 → 음성 → 음성 추가 에서 설치하세요.
#
# [Dialogflow 사용 시]
#   서비스 계정 JSON 키 파일을 스크립트와 같은 폴더에 배치:
#     avis-fcwa-d608a6b1f702.json
#   미배치 시 자동으로 DB 키워드 매칭 폴백 동작.
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── 음성 인식 (Google STT) ─────────────────────────────
SpeechRecognition>=3.10.0

# ── 마이크 입력 (PortAudio 바인딩) ─────────────────────
# Windows 휠은 PyPI 에 포함됨 (Python 3.10~3.13)
# 실패 시 pipwin 으로 설치: pipwin install pyaudio
PyAudio>=0.2.14

# ── 오프라인 TTS (Windows SAPI5 자동 사용) ─────────────
pyttsx3>=2.90

# ── 온라인 TTS (우선 사용, 캐시 지원) ───────────────────
# 코드가 자동/Edge TTS 설정일 때 edge-tts 를 우선 사용한다.
# Windows에서는 내장 PowerShell MediaPlayer 로 mp3 재생을 처리한다.
edge-tts>=7.0.0

# ── 메뉴 이미지 로딩·리사이즈 ──────────────────────────
Pillow>=9.1.0

# ── Dialogflow 자연어 인텐트 인식 ──────────────────────
google-cloud-dialogflow>=2.24.0

# ── (선택, 강력 권장) Windows SAPI5 직접 COM 접근 ───────
# 설치 시: PowerShell subprocess 없이 SAPI5 를 직접 호출해
#          TTS 응답 시간이 ~500ms → ~50ms 로 대폭 단축됨.
# 미설치 시: 기존 PowerShell System.Speech 방식으로 자동 폴백.
pywin32>=306

# ── (선택) Windows 콘솔 한글/색상 출력 안정화 ──────────
# colorama>=0.4.6
